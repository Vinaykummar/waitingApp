import 'package:flutter/material.dart';

class StoreSignUpPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Store Sign Up'),),
    );
  }
}
